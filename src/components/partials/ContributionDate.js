import '../../assets/styles/css/contribution-date.css'

const ContributionDate = (props) => {
    return (
        <p className="contribution-date">{props.date}</p>
    )
}

export default ContributionDate;